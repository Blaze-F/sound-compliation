databases:
  host: "localhost"
  port: 3306
  database: "tts_service"
  username: "db user name"
  password: "db pw"
  timezone: "+09:00"

secrets:
  django: "django secret"

token:
  secret: "jwt secret"
  expire_sec: 3600

pagenation:
  default_page_size: 10

audio_output:
  path: "audio_outputs/"
  make_folder: True